"""Sample module for AI test runner."""


def add_numbers(left: int, right: int) -> int:
    """Return the sum of two integers."""
    return left + right


def multiply_numbers(left: int, right: int) -> int:
    """Return the product of two integers."""
    return left * right
